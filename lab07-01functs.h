/** 
 * @brief Determines and prints the Residence Hall of a cadet given the cadet's squadron.
 * @param squad the squadron of the cadet
 * @return Returns 0 if input is good, 1 if otherwise
 */ 
int displayResidenceHall(int squad); 
